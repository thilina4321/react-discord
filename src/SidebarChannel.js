import React from 'react'
import './SidebarChannel.css'

function SidebarChannel({id, name}) {
    return (
        <div className="sidebar__channel">
            <div className="sidebar_channelName"> #Youtube  </div>
        </div>
    )
} 

export default SidebarChannel
